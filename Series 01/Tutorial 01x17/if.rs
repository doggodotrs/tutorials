fn main() {
    // conditions
    // let (x, y) = (1, 2);
    // let (x, y) = (2, 1);
    let (x, y) = (1, 1);

    // if expression
    if x > y {
        println!("{x} > {y}");
    } else if x < y {
        println!("{x} < {y}");
    } else {
        println!("{x} == {y}");
    }

}